import { SignupForm } from '@/components/forms/signup-form';

export const metadata = {
  title: 'Cadastre-se | Fullstack Chat',
  description: 'Criar uma conta no Fullstack Chat',
};

export default function SignupPage() {
  return <SignupForm />;
}
