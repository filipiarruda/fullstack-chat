import { LoginForm } from '@/components/forms/login-form';

export const metadata = {
  title: 'Login | Fullstack Chat',
  description: 'Entrar no Fullstack Chat',
};

export default function LoginPage() {
  return <LoginForm />;
}
